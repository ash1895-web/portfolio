import style from 'Section.module.css'

const Section = ({image, title, description, date, url}) => {
    return (
        <div>
            <div></div>
            <div></div>
        </div>
    )
}

export default Section